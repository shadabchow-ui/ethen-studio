"use client";

import "./boot";

/**
 * Studio V2 Job 13 — workbench slot boot host.
 *
 * Mounted once in the workbench layout; importing `./boot` registers the
 * nine Studio slot implementations with the shared shell contract.
 * Renders nothing.
 */
export function StudioWorkbenchBoot() {
  return null;
}
