/**
 * Canonical chat route endpoints.
 *
 * Declared with the runtime that serves them so a package test can name an
 * endpoint without importing a React hook out of the root monolith.
 */
export const CHATBOT_AGENT_CHAT_ENDPOINT = "/api/chatbot-agent/chat";
