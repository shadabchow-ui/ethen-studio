// RECONSTRUCTED ETHEN V2 SOURCE
// ORIGINAL FILE WAS NOT FULLY RECOVERABLE
// DO NOT REPRESENT THIS FILE AS FORENSICALLY RECOVERED SOURCE

import * as React from "react";
import { NavRow, type NavRowItem } from "./NavRow";
export function ProductionSidebar({ sections = [], collapsed = false, onToggleCollapsed, accountName, accountMeta, accountInitials, onOpenAccount }: { sections?: Array<{ label?: string; items: NavRowItem[] }>; collapsed?: boolean; onToggleCollapsed?: () => void; accountName?: string; accountMeta?: string; accountInitials?: string; onOpenAccount?: () => void; onOpenSearch?: () => void }) { return <aside data-v2-sidebar={collapsed ? "collapsed" : "expanded"} className="flex h-full shrink-0 flex-col border-r border-[var(--v2-border-subtle)] bg-[var(--v2-raised)] p-2" style={{ width: collapsed ? 56 : 256 }}><div className="min-h-0 flex-1 overflow-auto">{sections.map((section, index) => <div key={section.label ?? index} className="mb-4"><p className={collapsed || !section.label ? "sr-only" : "px-2 pb-1 text-[12px] font-medium text-[var(--v2-text-tertiary)]"}>{section.label}</p><div className="grid gap-1">{section.items.map((item) => <NavRow key={item.id} item={item} collapsed={collapsed} />)}</div></div>)}</div><button type="button" onClick={onOpenAccount} className="flex h-9 items-center gap-2 rounded-[var(--v2-radius-base)] px-2 text-left text-[12px] text-[var(--v2-text-secondary)]"><span className="grid h-5 w-5 place-items-center rounded-full bg-[var(--v2-hover)]">{accountInitials}</span>{collapsed ? null : <span className="min-w-0 truncate">{accountName}<small className="block truncate text-[var(--v2-text-tertiary)]">{accountMeta}</small></span>}</button><button type="button" onClick={onToggleCollapsed} className="mt-2 h-8 text-[12px] text-[var(--v2-text-tertiary)]">{collapsed ? "›" : "‹"}</button></aside>; }
