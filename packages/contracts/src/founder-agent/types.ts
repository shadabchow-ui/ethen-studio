/**
 * Founder-agent launch contract.
 *
 * Moved out of `lib/founder-agent/types.ts` so the credit outcome catalog can
 * name a launch mode without depending on the founder skill registry.
 */

export type CompanyLaunchMode = "new-company" | "grow-company";
